# Node Scripts Scaffold

Node-side helper scripts will live here.

Scripts must detect hardware and configuration at runtime. Do not copy values from the first developer machine profile into scripts.
