# Kiosk Scaffold

The kiosk will show exactly one SecondWind-controlled state:

- unpaired pairing screen
- paired idle/status screen
- connected streaming client

The Phase 0 manual `cage` command is evidence only. v0.1 should replace it with a supervised first-party launch path.
